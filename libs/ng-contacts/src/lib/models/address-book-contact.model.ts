import {Contact} from "@illmade-knight/action-intention-protos/proto/user/v1/user";
/**
 * Defines the data structure for a single contact in the address book.
 * This interface is used throughout the ng-contacts library for all contact data. [cite: 58]
 */
export type AddressBookContact = Contact
